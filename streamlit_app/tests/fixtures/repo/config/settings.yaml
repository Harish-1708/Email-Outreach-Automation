# FIXTURE settings — mirrors the real config/settings.yaml shape but is
# owned by the test suite and must never change to track production.
shared_sheet_id: "fixture_sheet_id_0000000000"

email_accounts:
  default_account: "sales1"

default_campaign_settings:
  stage_wait_days:
    intro: 0
    followup1: 3
    followup2: 4
    followup3: 5
    followup4: 5

  sending:
    timezone: "Asia/Kolkata"
    window_start: "09:00"
    window_end: "17:00"
    delay_min_minutes: 3
    delay_max_minutes: 7
    daily_limit: 100
    sender_rotation: false

  reply_monitor:
    lookback_hours: 24
